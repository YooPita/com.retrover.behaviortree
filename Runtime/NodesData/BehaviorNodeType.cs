namespace Retrover.BehaviorTree
{
    public enum BehaviorNodeType
    {
        Leaf,
        Decorator,
        Sequence,
        Selector,
        ParallelSequence,
        ParallelSelector
    }
}
